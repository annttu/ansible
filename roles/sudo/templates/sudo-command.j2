# {{ ansible_managed }}

# allow sudo for {{ user }} {% if command %} with command {{ command }} {% endif %}

{{ user }} ALL = ( ALL ) {% if nopasswd %}NOPASSWD: {% endif %}{% if command %}{{ command }}{% endif %}
